import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-infogerances',
  templateUrl: './infogerances.html',
  styleUrl: './infogerances.css',
  standalone: true,
  imports: [CommonModule]
})
export class Infogerances {
  // Component logic can be added here if needed for future enhancements
}
